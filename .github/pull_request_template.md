## What changed

## Why

## Validation

- [ ] `make lint` passes
- [ ] `make test-cov` passes
- [ ] `results/*.json` / README tables updated, if training or eval code changed

## Checklist

- [ ] No ROMs, checkpoints outside LFS, or secrets committed
- [ ] New modules have tests; emulator-dependent tests use `@requires_emulator`
